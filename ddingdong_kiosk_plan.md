# 띵똥 — 수학 키오스크 화면 계획서

> 학원에 둔 수학 키오스크 "띵똥"에서 학생이 자기 수학 전광판을 보는 화면입니다.
> 기존 키오스크 학생 화면은 그대로 두고, 상단에 영어 화면과 똑같은 "수학 전광판" 칩만 얹습니다.
> 점수 계산과 저장은 쏘이지(앱)가 하고, 띵똥은 그 점수를 읽어서 보여주기만 합니다.

---

## 1. 띵똥이 하는 일

- 학생이 띵똥에서 기존처럼 키오스크를 이용합니다. (지금 하던 화면 그대로)
- 그 **학생 화면 상단에 "수학 전광판" 칩**이 하나 보입니다. (영어 화면과 똑같은 모양·동작)
- 칩을 누르면 상세 전광판이 떠오르고, X나 바깥 여백을 누르면 닫혀 원래 키오스크 화면으로 돌아갑니다.
- 즉, 키오스크 본래 화면을 가리지 않고 칩만 얹는 방식입니다.

> 영어 화면과 키오스크가 같은 칩·모달 동작으로 통일되어, 학생이 어느 화면에서든 똑같이 씁니다.

---

## 2. 화면 구성

- **화면**: 띵똥(수학 키오스크) 학생 화면
- **지금 모습**: 기존 키오스크 학생 화면 (지금 쓰던 그대로)
- **바꿀 동작**:
  - 화면 상단에 "수학 전광판" 칩(작은 알약 버튼, 야구공 아이콘 + 글씨)을 얹습니다.
  - 수학도 듣는 학생에게만 칩이 보이고, 안 듣는 학생에게는 칩이 없습니다.
  - 칩을 누르면 **상세 전광판이 모달로 떠오릅니다**: 이름 + 큰 전광판(스트라이크 주황 / 볼 초록 / 아웃 빨강) + 다음 목표 한 줄 + 최근 기록.
  - 보충 대상이면 모달 안에 "보충 예정"을 분명하게 표시합니다.
  - 모달의 **X 버튼** 또는 **바깥 어두운 여백**을 누르면 닫힙니다.
  - 카드 **안쪽**을 눌렀을 때는 닫히지 않습니다. (실수로 닫히지 않게)
- **다 됐다고 볼 기준**:
  - 기존 키오스크 화면은 그대로 있고, 상단에 칩이 보인다.
  - 수학 듣는 학생에게만 칩이 보인다.
  - 칩을 누르면 상세 전광판이 뜬다.
  - X와 바깥 여백을 누르면 닫히고, 카드 안쪽을 누르면 안 닫힌다.
  - 볼의 점과 글씨가 초록으로 보인다.

---

## 3. 영어 화면과 같은 부품을 그대로 씀

- 키오스크 칩과 모달은 쏘이지 앱의 "수학 전광판 칩 / 전광판 모달"과 **똑같은 부품**을 재사용합니다.
- 새로 만들지 않고 같은 것을 가져다 상단에 얹기만 합니다. (모양·동작·색 모두 동일)
- 차이는 단 하나 — 키오스크는 화면이 크니 칩과 모달을 조금 더 크게 보여줄 수 있습니다.

---

## 4. 점수는 어디서 오나

- 모든 점수 계산(스트라이크 3 → 아웃 1, 볼 4 → 아웃 1 삭제 등)은 **쏘이지가 합니다.**
- 띵똥은 쏘이지에 저장된 그 학생의 현재 스트라이크·볼·아웃을 **읽어서 보여주기만** 합니다.
- 띵똥에서는 점수를 바꾸지 않습니다. (볼 주기·취소·면제는 선생님이 쏘이지에서)

---

## 5. 표시를 이해하기 위한 규칙 요약

- 스트라이크 3개 → 아웃 1개
- 볼 4개 → 아웃 1개 삭제
- 아웃 3개 → 보충 수업

> 자세한 규칙과 결정값(초기화 주기, 볼 개수 등)은 "수학야구규칙 운영 계획서"를 따릅니다.

---

## 6. 색과 부품

- 수학 전광판 칩: 작은 알약 버튼, 꿀색 톤, 야구공 아이콘 + "수학 전광판" (영어 화면과 동일)
- 전광판 모달: 어두운 배경 오버레이 + 가운데 상세 전광판 카드 + 우측 위 X
- 전광판 패널: 어두운 바탕 (#3A2A1A)
- LED 점
  - 볼(좋은 행동): 초록 #34C759 / 글씨 #5DD67F
  - 스트라이크(주의): 주황 #FF7A3D
  - 아웃(위험): 빨강 #FF4438
  - 꺼진 칸: 어두운 회색 #5A4630
- 모달 안 순서: 이름 → 큰 전광판 → 목표 한 줄 → 최근 기록

---

## 7. 모달 동작 상세 (변수의 변수)

- 열릴 때: 뒤 배경이 어두워지고 전광판 카드가 가운데에.
- 내용이 길면: 모달 안에서만 위아래로 넘기고 뒤 화면은 고정.
- 여러 번 눌러도 중복으로 쌓이지 않음.
- 일정 시간 손대지 않으면(키오스크 특성) 자동으로 닫혀 다음 학생을 위해 원래 화면으로 — 시간은 지현님이 정함.
- 접근성: 점(색)만으로 구분되지 않게 옆에 숫자(2/3)도 함께. 키오스크 글씨는 멀리서도 보이게 크게.

---

## 8. 빠질 수 있는 상황 (엣지 케이스)

- **본인 확인이 안 될 때**: 칩을 눌러도 "다시 확인해 주세요" 안내
- **점수를 불러오는 중**: 점 자리에 회색 깜빡임(스켈레톤) 잠깐
- **불러오기 실패**: "점수를 불러오지 못했어요. 잠시 후 다시 시도해 주세요."
- **점이 하나도 없을 때**: 빈 점만 보이고 목표는 "아직 깨끗해요. 이대로 가요!"
- **이름이 너무 길 때**: 한 줄로 줄이고 끝에 … 처리
- **보충 대상일 때**: 모달 안에 "보충 예정"을 분명하게

---

## 만들 순서

1. 쏘이지에서 점수가 제대로 계산·저장되는지 먼저 확인 (쏘이지 문서 참고)
2. 쏘이지의 영어 화면 칩·모달이 완성되면, 같은 부품을 키오스크 학생 화면 상단에 얹기
3. 키오스크 크기에 맞게 칩·모달을 조금 더 크게, 자동 닫힘 시간 등 마무리

---

## 9. 쏘이지 ↔ 띵똥 연동 (구현됨)

쏘이지가 점수를 **밀어넣고(쓰기)**, 띵똥은 학생 화면에서 **읽어서(읽기)** 보여줍니다.
포인트 연동(`/api/points/external`)과 같은 서비스키 방식이라 새로 만들 인증이 없습니다.

### 쓰기 — 쏘이지에서 점수가 바뀔 때마다 호출

```
POST https://bakuum-kiosk.pages.dev/api/math-board/external
Header:  X-Service-Key: <EXTERNAL_POINTS_KEY 와 동일>
         Content-Type: application/json
Body:
{
  "name": "김서연",          // 키오스크 학생 이름과 똑같이 (매칭 키)
  "strike": 2,                // 0~3
  "ball": 3,                  // 0~4
  "out": 1,                   // 0~3
  "goal": "볼 1개만 더 모으면 아웃 하나가 사라져요!",  // (선택) 비우면 규칙으로 자동 문구
  "supplement": false,        // (선택) 보충 예정이면 true
  "classLabel": "수학 화·목 7시반",  // (선택) 이름 밑 한 줄
  "records": [                // (선택) 최근 기록, 최신순 (최대 20개, 화면엔 6개)
    { "type": "ball",   "label": "자진 등원 학습", "date": "6/22" },
    { "type": "strike", "label": "숙제 미제출",   "date": "6/18" }
  ],
  "active": true              // (선택) false면 칩 숨김 = 수학 안 듣는 학생/졸업 처리
}
```

- 한 학생의 현재 상태를 **통째로 덮어씁니다**(증감이 아님). 쏘이지가 계산한 최종값을 그대로 보내면 됩니다.
- 점수가 바뀔 때마다 보내면, 학생이 모달을 열어둔 사이에도 **3초 안에 부드럽게** 점이 바뀝니다.
- `name` 이 키오스크 학생 명단에 없어도 저장은 됩니다(이름만 같으면 매칭). 안전하게 키오스크 이름과 동일하게 보내 주세요.

### 읽기 — 띵똥이 자동으로 호출 (별도 작업 불필요)

```
GET https://bakuum-kiosk.pages.dev/api/math-board?name=김서연
→ { "success": true, "exists": true, "board": { strike, ball, out, goal, supplement, classLabel, records, updatedAt } }
```

- 학생을 고르면 띵똥이 이 API로 전광판이 있는지 확인해 **칩을 보일지 결정**합니다.
- 칩을 누르면 모달이 열리고, 열려 있는 동안 3초마다 다시 읽어 **실시간으로** 갱신합니다.
- `active:false` 이거나 데이터가 없으면 칩이 안 보입니다(수학 안 듣는 학생).

### 동작 정리

- 자동 닫힘: 카드를 손대지 않으면 **45초** 뒤 자동으로 닫힘 (코드 `MB_IDLE_MS`).
- 실시간 갱신 주기: **3초** (코드 `MB_POLL_MS`), 메뉴 화면 칩 확인 **12초** (`MB_CHIP_MS`).
- 닫기: X 버튼 / 바깥 어두운 여백 → 닫힘, 카드 안쪽 → 안 닫힘.
- 엣지: 불러오는 중 스켈레톤, 실패 시 안내 문구, 데이터 없으면 칩 자체가 안 뜸.

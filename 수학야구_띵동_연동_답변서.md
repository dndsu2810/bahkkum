# 수학야구 띵동 ↔ 쏘이지 연동 — 답변서 (띵동 → 쏘이지)

> 작성: 띵동(수학 키오스크) 측
> 대상: 쏘이지(박꿈수학앱)
> 받은 문서: `수학야구_띵동_연동요청서.md` 에 대한 답변 + 띵동 구현 현황

---

## 0. 한 줄 요약

**→ 방향: PULL 로 확정했습니다.** (띵동이 쏘이지 board API를 읽어감)

요청서대로 **띵동이 board를 읽어서 그대로 그리도록** 구현 완료했습니다.
인증·단말 보안 때문에 **키오스크가 쏘이지를 직접 부르지 않고, 띵동 서버가 대신 호출(프록시)** 합니다.
쏘이지는 아래 하나만 열어주시면 됩니다:
```
GET /api/baseball/board?student_id=<로스터ID>
Header: X-Read-Token: <발급해줄 읽기 토큰>
→ 응답: { board: {...}, photo }   (3번 스키마)
```
(PUSH 방식 코드도 남겨뒀지만, 이번엔 PULL로 갑니다.)

---

## 1. 협의 사항 답변 (요청서 4번)

### ① 인증 — **제안 A(키오스크 전용 읽기 토큰) 채택 + 띵동 서버 프록시**

- 띵동은 학원에 둔 **무인 공용 단말**이라, 단말 브라우저에 회사 세션/토큰을 두면 안 됩니다.
- 그래서 흐름을 이렇게 했습니다:

  ```
  키오스크(브라우저)  →  띵동 서버  →(X-Read-Token)→  쏘이지 /api/baseball/board
                       (토큰은 띵동 서버 환경변수에만 보관, 단말엔 절대 노출 X)
  ```

- **쏘이지가 해줄 것**: 키오스크 전용 **읽기 토큰** 1개 발급 → `GET /api/baseball/board` 만 허용.
  - 토큰 전달 헤더는 `X-Read-Token` 으로 기본 설정했습니다. (다른 헤더명이 편하면 알려주세요, 맞추겠습니다.)
- 띵동은 이 토큰을 `SOEZ_READ_TOKEN` 환경변수로 보관합니다. → 제안 B(등하원 때 같이 내려주기)는 안 써도 됩니다.

### ② 학생 ID 매핑 — **띵동 `students.id` 사용**

- 띵동은 수업관리 앱과 공유하는 `class_student_meta(student_id, online_id, ...)` 를 이미 들고 있고, `student_id` = 띵동 `students.id` 입니다.
- 띵동은 board 호출 시 이 값을 `student_id` 로 보냅니다: `?student_id=<students.id>`.
- **확인 필요**: 이 `student_id` 가 쏘이지의 **로스터ID와 동일**한가요?
  - 동일하면 그대로 진행.
  - 다르면, `class_student_meta.online_id` ↔ 쏘이지 계정으로 매핑하는 표를 한 번 맞추면 됩니다. (어느 쪽이 편한지 알려주세요.)

### ③ 갱신 주기

- 띵동은 **학생이 전광판 모달을 연 동안만 3초 폴링** 합니다(보는 중 실시간 반영). 닫으면 폴링 중단.
- 메뉴 화면 칩은 항상 노출, 모달 안 보면 호출 없음. → 쏘이지 부하 거의 없음.
- 요청서의 "등하원 이벤트 때 1회 조회면 충분"에도 동의합니다. 다만 학생이 직접 들여다보는 화면이라 모달 동안만 짧게 폴링합니다.

---

## 2. 띵동이 구현한 것 (현재 배포 완료)

두 방향 **둘 다** 지원합니다. 쏘이지가 편한 쪽을 고르면 됩니다.

### 방식 A — PULL (요청서대로, 권장)
- 띵동에 `SOEZ_BASE_URL`, `SOEZ_READ_TOKEN` 환경변수가 설정되면, 띵동이 쏘이지의 board API를 서버에서 호출해 그대로 표시합니다.
- 띵동이 호출하는 형태:
  ```
  GET {SOEZ_BASE_URL}/api/baseball/board?student_id=<로스터ID>
  Header: X-Read-Token: <발급 토큰>
  ```
- 기대 응답: **요청서 2번의 그 JSON** (`{ board: {...}, photo }`). 아래 3번 스키마와 동일.

### 방식 B — PUSH (쏘이지가 띵동으로 쏘기, 이미 동작 중)
- 쏘이지가 점수 바뀔 때마다 띵동으로 보냄:
  ```
  POST https://bakuum-kiosk.pages.dev/api/math-board/external
  Header: X-Service-Key: <기존 포인트 연동 키와 동일>
  Body: 아래 3번 스키마 (S/B/O, penaltyRounds, pendingMakeup, honey, status, recent, history ...)
  ```
- 진단용: `GET https://bakuum-kiosk.pages.dev/api/math-board/debug?key=<서비스키>` 로 도착/오류 확인 가능.

> 현재는 방식 B로 즉시 쓸 수 있고, 쏘이지가 board API를 열면 환경변수만 넣어 방식 A로 전환됩니다(코드 수정 없음).

---

## 3. 확정 스키마 (띵동이 읽는/그리는 형태)

요청서 2번과 동일하게 맞췄습니다. 띵동이 그리는 데 쓰는 키:

```jsonc
{
  "board": {
    "studentId": "123",
    "S": 2, "B": 1, "O": 0,         // 스트라이크·볼·아웃 (없으면 strike/ball/out 도 허용)
    "penaltyRounds": 1,              // 현재 회차 = penaltyRounds + 1 로 표시
    "pendingMakeup": false,          // true면 "⚠️ 보충 대상" 배지
    "honey": 0,                      // >0 이면 "🍯 꿀 N" 배지
    "status": "warn",               // clean | good | warn | makeup (없으면 띵동이 추정)
    "monthLabel": "2026-06",
    "classLabel": "수학 화·목 7시반", // 이름 밑 한 줄 (선택)
    "goal": "스트라이크 하나만 더 쌓이면 아웃이에요. 조심!", // 없으면 띵동이 규칙으로 자동 문구
    "recent":  [ /* 최근 6~8건, 아래 형태 */ ],
    "history": [ /* 전체 최신순(최대 120), round로 묶어 "회차별 기록 보기" */ ]
  },
  "photo": "https://.../up/xxxx.jpg"  // 없으면 "" (그러면 이름 첫 글자 아바타)
}
```

기록 한 건:
```jsonc
{ "date": "2026-06-22", "tone": "ball", "label": "자진 등원 학습", "delta": "볼 +1", "round": 2 }
// tone: strike | ball | minus | makeup | honey   (없으면 회색)
```

- 수학 시간대가 아니거나 데이터가 없으면 `board: null`(PULL) 또는 미존재(PUSH) → 띵동은 **빈 전광판(깨끗)** 으로 표시합니다.
- 색/문구는 요청서 3번과 동일하게 적용했습니다(스트라이크 #FF7A3D · 볼 #34C759 · 아웃 #FF4438 · 패널 #3A2A1A · 꺼진 칸 #5A4630, 각 줄 개수 숫자 표기).

---

## 4. 쏘이지가 우리에게 줘야 할 것 (PULL로 갈 경우)

1. `SOEZ_BASE_URL` — 쏘이지 API 베이스 (예: `https://app.soez...`)
2. `SOEZ_READ_TOKEN` — 키오스크 전용 읽기 토큰
3. 헤더명 확인 (`X-Read-Token` 으로 맞춰둠 — 다르면 알려주세요)
4. `student_id` 가 쏘이지 로스터ID와 동일한지 확인

띵동 쪽 설정(우리가 함):
```
npx wrangler pages secret put SOEZ_BASE_URL --project-name bakuum-kiosk
npx wrangler pages secret put SOEZ_READ_TOKEN --project-name bakuum-kiosk
```

> PUSH로 계속 갈 거면 위 4개는 필요 없고, 지금처럼 `/api/math-board/external` 로 보내면 됩니다.

문의: 띵동(지현)
